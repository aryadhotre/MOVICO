# --- web layer ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# --- persistence ---
sqlalchemy>=2.0.30
# Only needed when USE_SQLITE=false; SQLite is the default.
psycopg2-binary>=2.9.9

# --- auth ---
bcrypt>=4.1.0
pyjwt>=2.8.0

# --- numerics / modelling ---
numpy>=1.26.0,<2.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.2.0
# Arrow's threaded CSV reader loads the 891MB ratings file in ~1s; the pandas
# engine takes minutes and several GB.
pyarrow>=15.0.0

# --- io ---
httpx>=0.27.0
requests>=2.31.0
python-dotenv>=1.0.1

# --- optional shared cache tier (REDIS_ENABLED=true) ---
redis>=5.0.0

# --- tests ---
pytest>=8.0.0
